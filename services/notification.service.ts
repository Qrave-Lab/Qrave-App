/**
 * Notification service — re-exports notification context for easy imports.
 */
export { NotificationContext, NotificationProvider } from '@/contexts/NotificationContext';
export type { AppNotification, NotificationContextValue } from '@/types/notification';
